TODO list:


Make possible to tap on survey to get closest point on survey

Add pathfinding that may take into account closest points if actual destination cannot be reached
Add pathfinding that takes into account avoiding certain edges

Make demo
show all weights for paths
three tickboxes for flags
two buttons for changing destination
path changes depending on flags

source has its own icon
toggle cave route lines on map
specific path distance area details has same colour as route colour

when chagne source or cancel route, do not reset transformations

left handled vs right handed layout
icon buttons and text buttons aren't mixed they have separate areas


Make landing page i.e. list of surveys to select
Make room database
make icon and lines scale with size of image and box (if zoom in, box stays same size on screen, doesn't get bigger)
